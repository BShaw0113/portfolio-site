# Portfolio Site

This repository contains a static personal portfolio website built with plain HTML, CSS, and JavaScript.

## Structure
- `index.html` and related page files define site content.
- `styles.css` contains shared styling.
- `script.js` includes shared interactive behavior.

## Local preview
Open `index.html` directly in a browser, or serve the folder with any static file server.
